<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="single">
        <item android:id="@+id/nav_home_button"
            android:icon="@drawable/ic_home"
            android:title="Home"
            android:checked="true"
            />
        <item android:id="@+id/nav_favourite_button"
            android:title="Favourite"
            android:icon="@drawable/ic_white_favourite_on"
            />
        <item android:id="@+id/nav_trash_button"
            android:title="Trash"
            android:icon="@drawable/ic_white_trash_off"
            />

        <item android:id="@+id/nav_support_us_button"
            android:title="Support Us"
            android:icon="@drawable/ic_white_favourite_on"
            />

        <item
            android:enabled="false"
            android:title="Night Mode"
            app:actionLayout="@layout/custom_night_mode_switch"
            android:icon="@drawable/ic_night_mode"/>
    </group>

</menu>
